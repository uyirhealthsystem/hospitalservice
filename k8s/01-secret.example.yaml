# Template only - DO NOT commit real values or apply this file as-is.
#
# Preferred: use External Secrets Operator to sync this from AWS Secrets Manager
# instead of hand-creating it. For a quick start:
#   kubectl create secret generic hospital-service-secrets \
#     --namespace uyir \
#     --from-literal=SPRING_MONGODB_URI='mongodb+srv://user:pass@cluster/hospital'
apiVersion: v1
kind: Secret
metadata:
  name: hospital-service-secrets
  namespace: uyir
type: Opaque
stringData:
  SPRING_MONGODB_URI: "mongodb+srv://<user>:<password>@<cluster-host>/hospital?appName=<app>"
